'use client'
import React from 'react'

const addToCart = () => {
  return (
    <div>
        <button className='btn btn-primary' onClick={() => console.log("Add to the cart")}>Add</button>
    </div>
  )
}

export default addToCart